原仓库 https://github.com/Python3WebSpider/DeepLearningSlideCaptcha2

###### 第一个bug
tensorflow==2.21 (Mar 7, 2026)  没有2.24版本

###### 第二个bug
chrome 版本要与chromedriver匹配

###### 第三个bug
collect.py中打开网站没有自动填写用户名和密码
点击登录按钮无反应 

###### 第四个bug
```shell
运行labelImg出现bug 在网上搜索遇到该问题可能是 python 版本过高不发生自动类型转换
Traceback (most recent call last):
  File "\Python3.11\Lib\site-packages\libs\canvas.py", line 530, in paintEvent
    p.drawLine(self.prev_point.x(), 0, self.prev_point.x(), self.pixmap.height())
TypeError: arguments did not match any overloaded call:
```

修改源代码为下面进行整型转换
```text
526      p.drawRect(int(left_top.x()), int(left_top.y()), int(rect_width), int(rect_height))
530      p.drawLine(int(self.prev_point.x()), 0, int(self.prev_point.x()), int(self.pixmap.height()))
531      p.drawLine(0, int(self.prev_point.y()), int(self.pixmap.width()), int(self.prev_point.y()))


```

下面的命令无实践

prepare.sh train.sh detect.sh linux的命令 
在windows使用Bash命令行(如Git Bash)工具运行此命令
